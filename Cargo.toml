[package]
name = "wire-probe"
version = "0.1.0"
edition = "2021"

[dependencies]
io-uring = "0.6"
pico-args = "0.5"
itoa = "1.0"
ryu = "1.0"
libc = "0.2"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
